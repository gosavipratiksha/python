#Creating a list using []
a = [1,42,6,23,7,89]

#Print the list using print() function
print(a)

#Access using index using a[0], a[1], a[2]
print(a[2])

#Change the value of list using
a[0] = 87
print(a)

#We can create a list using items of different types
a = (78, "hey", True, 4.7)
print(a)
print(type(a))

#NOTE=Tuple is used to store multiple items in a single variable
